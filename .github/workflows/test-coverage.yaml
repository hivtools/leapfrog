# Workflow derived from https://github.com/r-lib/actions/tree/master/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches:
      - main
  pull_request:

name: test-coverage

jobs:
  test-coverage:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    defaults:
      run:
        working-directory: leapfrogr

    steps:
<<<<<<< HEAD
      - uses: actions/checkout@v4
=======
      - uses: actions/checkout@v6

      - name: Generate C++ headers
        uses: ./.github/actions/setup
>>>>>>> 11ae9f4be0fefc897892a8eb6689b4043c7c0bd7

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
<<<<<<< HEAD
          extra-repositories: https://mrc-ide.r-universe.dev
=======
          extra-repositories: "https://mrc-ide.r-universe.dev https://hivtools.r-universe.dev"
>>>>>>> 11ae9f4be0fefc897892a8eb6689b4043c7c0bd7

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: covr, any::here
          needs: coverage
          working-directory: leapfrogr

      - name: Create test data
        shell: bash
        run: ./scripts/create_test_data.R
        working-directory: .

      - name: Test coverage
        run: |
          covr::codecov(
            quiet = FALSE,
            clean = FALSE,
            install_path = file.path(Sys.getenv("RUNNER_TEMP"), "package")
          )
        shell: Rscript {0}

      - name: Show testthat output
        if: always()
        run: |
          ## --------------------------------------------------------------------
          find ${{ runner.temp }}/package -name 'testthat.Rout*' -exec cat '{}' \; || true
        shell: bash

      - name: Upload test results
        if: failure()
<<<<<<< HEAD
        uses: actions/upload-artifact@v4
=======
        uses: actions/upload-artifact@v6
>>>>>>> 11ae9f4be0fefc897892a8eb6689b4043c7c0bd7
        with:
          name: coverage-test-failures
          path: ${{ runner.temp }}/package
